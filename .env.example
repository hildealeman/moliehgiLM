# MolieLM - Environment Template
# Copia este archivo a:
# - .env.local (desarrollo local)
# - o configura estas variables en Vercel (Production/Preview)

# =========================
# SELECTORES DE PROVEEDOR
# =========================

# Storage/Data provider:
# - local    => Mock Mode (IndexedDB/localStorage)
# - supabase => Cloud Mode (Supabase Postgres + Storage)
VITE_DATA_PROVIDER=local

# AI provider:
# - gemini_client => Gemini directo desde el navegador (NO recomendado en producción)
# - gemini_proxy  => Llama a Supabase Edge Function 'gemini-proxy' (RECOMENDADO prod)
VITE_AI_PROVIDER=gemini_proxy

# =========================
# SUPABASE (FRONTEND)
# =========================
# Requerido si:
# - VITE_DATA_PROVIDER=supabase  o
# - VITE_AI_PROVIDER=gemini_proxy
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# =========================
# GEMINI (FRONTEND - OPTIONAL)
# =========================
# Solo si usas VITE_AI_PROVIDER=gemini_client.
# Esta app puede leer la key desde:
# - API_KEY (inyectada por Vite en build via vite.config.ts -> process.env.API_KEY)
# - o VITE_API_KEY (fallback)
#
# En producción, evita exponer keys en el cliente. Usa gemini_proxy.
# API_KEY=YOUR_GEMINI_API_KEY
# VITE_API_KEY=YOUR_GEMINI_API_KEY

# =========================
# RATE LIMITING (PRODUCTION)
# =========================
# Configuración de límites de uso para controlar costos
# Estas variables se configuran en Supabase Edge Function secrets
# GEMINI_DAILY_TOKEN_LIMIT=1000000
# GEMINI_REQUESTS_PER_HOUR=100